pipeline {
    agent {label '35.240.70.244'} 
    stages {
        stage('Install') {
             steps {
                sh 'npm install'
            }
        }
        
        stage('Test') {
             steps {
                sh 'npm test'
            }
        }
        
        stage('Build') {
             steps {
                sh 'npm run build'
            }
        }
        
        stage('Archive artifacts') {
             steps {
            archiveArtifacts '*.zip'    
            }
        }
    }
}
